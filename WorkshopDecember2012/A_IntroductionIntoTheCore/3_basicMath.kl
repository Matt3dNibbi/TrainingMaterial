

operator addOp(
  Scalar a,
  Scalar b,
  io Scalar result
) {
  result = a + b;
  report("Added "+a+" and "+b+" to "+result);
}

operator mulOp(
  Scalar a,
  Scalar b,
  io Scalar result
) {
  result = a * b;
  report("Multiplied "+a+" and "+b+" to "+result);
}